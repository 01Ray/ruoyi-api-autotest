[pytest]
# 测试文件搜索路径
testpaths = api_tests/cases

# 测试文件命名规则
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 输出选项
addopts = 
    -v
    --tb=short
    --strict-markers
    --color=yes

# 自定义标记
markers =
    smoke: 冒烟测试
    regression: 回归测试
    login: 登录模块
    user: 用户模块
    role: 角色模块